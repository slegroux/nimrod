# @package _global_

defaults:
  - _self_
  - callbacks: default
  - datasets: mnist
  - dataloaders: default
  - trainer: default
  - paths: default
  - hydra: default
  - logger: wandb
  - profiler: simple
  - model: autoencoder

run_id: ${now:%Y-%m-%d}_${now:%H-%M-%S}
project: "autoencoder"
ckpt_path: null # ckpt to resume training from
seed: 42
test: True
train: True
ignore_warnings: True
tags: ["dev"]